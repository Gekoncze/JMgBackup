### NOW ###
    ...

### SOON ###
    add tree model entry renderer
        show error icon for nodes with errors

    add directory compare service
    add component to reload directory

### LATER ###
    add support for symbolic links

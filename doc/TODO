### NOW ###
    how to handle exception severities
        add more icons for various categories
            black for unexpected errors (usually file system exceptions)
            red will stay for critical errors (missing files and directories)
            orange will be for warning (duplicates and moves)
            blue possibly for information exceptions (if needed)
        implement icons for various severities
            need to handle nested exceptions correctly

### SOON ###
    could it happen that cancel exception could be caught and not propagated during some complex operation?
        we may need to exclude it from being caught

    move detector result might be always hidden by missing file detection
        consider either removing it or overriding missing file exception
        also, I think we already compare root node name, so we might as well not skip its check in move detection

    finalize duplicate detector
        clear duplicate and move exceptions before file matcher starts ???
            similar was for comparator !!!
        run duplicate detector after compare
        add context menu to show duplicate suspects in dialog
        add dialog and/or menu to configure duplicate detection

    copy missing files enhancements
        show size in bytes instead of steps
        show currently copied file

    add relocate moved files action
        for each selected file:
            * make sure that target file won't collide with existing file
            * make sure that both file contents are equal
                * may need to introduce file comparator
                    implement standalone data comparison per bytes for precise detection between found suspects
                * may need to rename existing file comparator to file metadata comparator
                * may need to rename existing directory comparator to directory metadata comparator
            * move file using native filesystem move operation

### LATER ###
    add context menu for tree entries
        add option to reload / refresh specific file or directory
        add option to show error details

    allow user to show usage in percent relative to parent total
        might want to add preference to view menu
        consider showing colors (for example red high percent, green small percent)
        could be added to details view too

    add more ui unit tests
        add tests for wrapper tree nodes (don't forget root path that has null file name)
        add test for directory tree (restore expanded, collapsed and selected nodes)

### CONSIDERATIONS ###
    consider considering last modified date when recomputing checksum
        would require storing timestamp to checksum

    consider adding settings to not automatically compare directories
        in case comparison is not up to date, show information banner ???
        consider adding settings to completely disable directory compare

    consider adding settings to not automatically detect duplicates
        in case duplicates are not up to date, show information banner ???
        consider adding settings to completely disable duplicate detection

    consider showing disk usage somewhere near top (if possible)
        could then show warning when copy operation may fail because of not enough space in target device
        would need to detect device for path and read its properties somehow

    consider moving remaining logic from main window and directory view to directory manager
        such as reload and checksum actions in directory view
        actions over directories should be centralized if possible
        consider adding something like actions manager for ui layer
        consider if state should be stored in directory views or in some state object (wild idea)

    consider adding support for symbolic link copy
        could be nice to have a separate icon for it
        would need to add file type to file details view
        there is a problem of how to handle symlink paths:
            a) if outside of current work tree, what to do?
            b) if inside of current work tree, keep original symlink target or update it relative to target work tree?

    optimize directory search if needed
        currently iterating through all elements
        and also not utilizing sorted elements

    consider allowing multiple checksums types to be computed for a single file

    consider updatable progress description (or additional updatable field)
        possible use case: include processing speed updated in real time, such as kB/s

    consider allowing users to choose in main menu bar which kinds of errors they want to see and which to hide
        configurable via view menu:
            missing file or directory
            hash does not match
            possible duplicate of a file
            property not computed
            file system error
